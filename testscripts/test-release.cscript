#!./cmake-build-release/cscript
#gcc -lcrypto

#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    printf("Test script arguments: %d\n", argc);
    for (int i = 0; i < argc; i++) {
        printf("Arg #%d: %s\n", i, argv[i]);
    }
    system("pwd");
    return 0;
}

